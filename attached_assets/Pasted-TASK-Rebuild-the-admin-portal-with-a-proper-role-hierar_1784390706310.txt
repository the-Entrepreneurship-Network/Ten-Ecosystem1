TASK: Rebuild the admin portal with a proper role hierarchy, remove the old 
admin auth/backdoor entirely, and open a PR. Follow every step in order. 
Do not skip discovery steps. Do not deploy directly to production at any point.

═══════════════════════════════════════════════════════════════
STEP 0 — SAFETY PRECONDITIONS (do not proceed past this step until done)
═══════════════════════════════════════════════════════════════
1. Confirm which directory is the LIVE production process (`pm2 list`, 
   `pm2 show <name>`, check cwd) vs. which is the git-managed checkout 
   (~/ten-portal-production) vs. staging (~/ten-portal-staging). State all 
   three paths explicitly before touching anything.
2. Run the existing backup script now: ~/backup-mongo.sh — confirm it 
   completed and show me the backup file timestamp before continuing.
3. Do all work on a new branch off `develop`, named `feature/admin-rbac-rebuild`. 
   Never commit or push directly to `main`.
4. All work in this task happens against ten-portal-staging / the develop 
   branch first. Production is NOT touched until I explicitly approve after 
   reviewing staging.

═══════════════════════════════════════════════════════════════
STEP 1 — ROLE MODEL
═══════════════════════════════════════════════════════════════
Hierarchy, lowest to highest:
  Student/Intern (existing Student model — no admin access, unchanged)
  Coordinator (existing Coordinator model — unchanged business logic)
  HR — 8 levels: hr_1 through hr_8 (lowest to highest)
  Admin
  Founder (exactly one account)

Do NOT create a new "AdminUser" collection from scratch. Instead:
- Add a new model `models/AdminUser.js` for Founder + Admin ONLY (small, 
  since these are rare/high-privilege accounts):
    {
      username: String (unique, required),
      email: String (unique, required, lowercase),
      passwordHash: String (required, bcrypt),
      role: { type: String, enum: ['founder','admin'], required: true },
      isActive: { type: Boolean, default: true },
      createdBy: String,  // username of whoever created this account
      lastLogin: Date,
      createdAt: { type: Date, default: Date.now }
    }
- Extend the EXISTING `models/HR.js` — change the `role` field's usage: it 
  currently defaults to the string "hr" and is unused for tiering. Change it 
  to store one of: 'hr_1','hr_2','hr_3','hr_4','hr_5','hr_6','hr_7','hr_8'. 
  Add a migration step (see Step 6) to set any existing HR records without a 
  valid level to 'hr_1' (safest default — do not guess a higher level for 
  existing accounts).
- Do NOT modify `models/Coordinator.js` structurally — its existing fields 
  and verificationStatus flow stay exactly as-is.
- Do NOT touch anything under controllers/contractorController.js, 
  models/ContractorProfile.js, routes/contractorRoutes.js, or the contractor 
  HTML pages. Leave the contractor system completely alone — it's an 
  unrelated, not-yet-decided track.

Permission table (each level includes everything below it):
  hr_1 (Associate)        — view students (read-only)
  hr_2 (Support)           — + view payments/certificates (read-only)
  hr_3 (Verifier)          — + approve/reject certificate requests
  hr_4 (Finance)           — + verify/reject payments (approve/reject/export)
  hr_5 (Account Manager)   — + reset student passwords, unlock student accounts
  hr_6 (Coordinator Lead)  — + manage coordinators (view, reset password, delete)
  hr_7 (Comms Lead)        — + send broadcast notifications
  hr_8 (HR Head)           — + view audit log, recalculate attendance
  admin                    — + manage HR accounts (create/edit level/delete), 
                              delete students, all of the above
  founder                  — + create/delete Admin accounts, everything above.
                              Only role that can create another founder-level 
                              or admin-level account.

═══════════════════════════════════════════════════════════════
STEP 2 — AUTH (replaces the old system entirely — apply the same strict 
rules as the previous login fix)
═══════════════════════════════════════════════════════════════
Create middleware/adminAuth.js from scratch (delete the old one):

- Login checks against AdminUser (founder/admin) OR HR (hr_1–hr_8) by 
  username+password, using bcrypt.compare against the stored hash. 
  Coordinators do NOT get admin-portal login — they keep their existing 
  separate coordinator login flow untouched.
- On success, store in session: { userId, username, role, level (numeric 
  1-10, where hr_1=1 ... hr_8=8, admin=9, founder=10), lastActivity }.
- Build `requireRole(minLevel)` middleware that checks 
  `req.session.adminUser.level >= minLevel` and returns 401 if not, same 
  session-timeout logic as the existing requireAdminAPI (30 min).

ABSOLUTE RULES — same as before, no exceptions:
- NO hardcoded fallback passwords, NO "contains admin" username matching, 
  NO unconditional bypass of any kind, under any comment/justification.
- NO logging of plaintext passwords anywhere (console, files, or DB).
- Delete login_attempts.log if it still exists, and delete any code that 
  writes to it.
- Every password comparison uses bcrypt.compare() — nothing else.
- If ADMIN_PORTAL_PASSWORD env var still exists anywhere in code, remove 
  that dependency — passwords now live only as bcrypt hashes in the DB.

═══════════════════════════════════════════════════════════════
STEP 3 — SEED THE FOUNDER ACCOUNT
═══════════════════════════════════════════════════════════════
Write a one-time seed script (scripts/seed-founder.js) that:
- Reads FOUNDER_USERNAME, FOUNDER_EMAIL, FOUNDER_PASSWORD from environment 
  variables (do NOT hardcode any password in the script itself).
- Refuses to run if a founder account already exists (idempotent safety).
- Hashes the password with bcrypt and inserts the AdminUser record with 
  role: 'founder'.
- Print a clear success/failure message, never print the password back.
Do not run this script yet — I will run it myself with my own env vars once 
the PR is ready to test (see manual steps below).

═══════════════════════════════════════════════════════════════
STEP 4 — RE-GATE EXISTING ROUTES (business logic unchanged)
═══════════════════════════════════════════════════════════════
In routes/adminPortal.js, replace every `requireAdminAPI` with 
`requireRole(N)` at the levels below. Do NOT change the logic inside any 
route handler — only the middleware guarding it.

  requireRole(9)  [admin+]  : /hrs (create/edit level/delete), /students DELETE
  requireRole(8)  [hr_8+]   : /audit-log, /attendance/recalculate-all
  requireRole(7)  [hr_7+]   : /notifications/broadcast
  requireRole(6)  [hr_6+]   : /coordinators (view/reset-password/delete)
  requireRole(5)  [hr_5+]   : /students/:id/unlock, /students/:id/reset-password, 
                                /students/:id PUT (edit)
  requireRole(4)  [hr_4+]   : /payments/verify/:id, /tenure-payment/approve, 
                                /tenure-payment/reject, /payments/export
  requireRole(3)  [hr_3+]   : /certificates/pending (approve/reject actions)
  requireRole(2)  [hr_2+]   : /payments/pending, /payments/all, 
                                /tenure-payment/pending, /certificates/history 
                                (read-only views)
  requireRole(1)  [hr_1+]   : /students (list/view, read-only), /stats, 
                                /system/info

Keep every route's existing request/response shape and business logic 
byte-for-byte identical — payment approval still triggers cert generation, 
notification, tenure unlock exactly as it does now; rejection still requires 
a reason and resets shortCoursePaymentId exactly as now; student deletion 
still blocks if a successful payment exists; everything else the same. 
This is a permissions change, not a rewrite of what these routes do.

═══════════════════════════════════════════════════════════════
STEP 5 — DELETE THE OLD ADMIN PORTAL FRONTEND
═══════════════════════════════════════════════════════════════
Find and remove the old admin HTML/JS/CSS under public/ (search for the 
admin login page, dashboard page, and any admin-specific JS files — likely 
named something like admin-login.html, admin-dashboard.html or similar — 
list what you find before deleting and confirm with me if anything looks 
ambiguous). Do NOT touch contractor-login.html, contractor-dashboard.html, 
or any student/HR/coordinator-facing pages that aren't part of the admin 
portal.

═══════════════════════════════════════════════════════════════
STEP 6 — BUILD THE NEW ADMIN PORTAL FRONTEND
═══════════════════════════════════════════════════════════════
Single login page + single dashboard shell that adapts its sidebar/sections 
based on req.session.adminUser.level (fetch role from /api/admin-internal/
session-check on load, render nav accordingly — don't build 9 separate 
dashboard files).

Requirements:
- Clean, modern, professional look — proper spacing, a real sidebar nav, 
  data tables with sorting/pagination for students/payments/HRs/coordinators, 
  not raw JSON dumps. Use the existing frontend stack/conventions already in 
  this codebase (check public/ for existing CSS framework or design 
  patterns and stay consistent with them, don't introduce a new one).
- Every section only appears in the nav if the logged-in user's level 
  qualifies for at least one route in it.
- Payment approval/rejection UI: pending queue with clear approve/reject 
  buttons, rejection requires typing a reason (matches the existing backend 
  validation), CSV export button, tenure-payment queue as its own tab — 
  same functionality as today, just better presented.
- Show a "your role: X (level Y)" indicator somewhere visible so it's 
  obvious who's logged in as what.

═══════════════════════════════════════════════════════════════
STEP 7 — MIGRATION FOR EXISTING HR RECORDS
═══════════════════════════════════════════════════════════════
Write scripts/migrate-hr-levels.js that sets `role: 'hr_1'` on any existing 
HR document that doesn't already have a valid hr_1–hr_8 value. Run it only 
after I approve, and report exactly how many records were updated.

═══════════════════════════════════════════════════════════════
STEP 8 — VERIFY ON STAGING
═══════════════════════════════════════════════════════════════
Deploy to ten-portal-staging only. Then:
1. Confirm the app starts clean (pm2 logs, no errors, MongoDB connected).
2. Confirm old admin login attempts (old password, old routes) no longer work.
3. Wait for me to run the seed-founder script and confirm I can log in.
4. Walk through one full payment approve and one reject on staging with real 
   test data and confirm both work exactly as before (cert generated on 
   approve, notification sent, reason enforced on reject).
Do not proceed to Step 9 until I confirm staging looks right.

═══════════════════════════════════════════════════════════════
STEP 9 — OPEN THE PULL REQUEST
═══════════════════════════════════════════════════════════════
Push the feature/admin-rbac-rebuild branch and open a PR against `develop` 
(NOT main) at growth-eng/Ten-Ecosystem1. PR description must list:
- Every file added/removed/modified
- The permission table from Step 1
- Explicit confirmation that no hardcoded/fallback/bypass credentials exist 
  anywhere in the diff (grep for "fallback", "bypass", "ALWAYS grant" and 
  paste the (empty) grep result as proof)
- A note that production has NOT been touched and requires a separate, 
  manual go-ahead

Use the GitHub token from the environment variable I've set (GITHUB_TOKEN) — 
do not print, log, or commit the token anywhere, including in commit 
messages or the PR body.